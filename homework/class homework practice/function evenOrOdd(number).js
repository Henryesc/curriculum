function evenOrOdd(number) {
    
      if (number % 2 === 0)
        return ("Even")
      else 
        return ("Odd")
  }

  // this is saying that if the number is divivsable by 2 and = 0 return it as "even"
  //if it isnt divisable by 2 return it as "odd"

const evenOrOdd = number => number % 2 ? 'Odd' : 'Even';

//the "?" means is this condition true if it is do this if it isnt true do this 